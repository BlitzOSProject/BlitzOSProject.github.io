باید `FileStuff.h` و `FileStuff.c` را به پروژه‌اتان اضافه کنید. با اینکار امکان نوشتن و خواندن از دیسک و مدیریت فایل هارا بدست می‌آورید.
> این کد‌ و اینترفیس‌ها به طور کامل پیاده‌سازی شده‌اند و نیازی به تغییر در آن ها نیست.

### Task 1



### Task 2



### Task 3

برای Task2 به کد‌های Kernel.h رجوع کنید. همچنین حواستان باشد که تمام مقادیر ورودی عددی نیستند، بعضی پوینتری به رشته در user-space هستند.

برای Task3 دقت کنید که پیاده‌سازی SystemCall ‍‍`Exec` کافی می‌باشد.
> از 
